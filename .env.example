# Toggle Lab Mode (non-prod dev recommended)
LAB_MODE=0

# Optional key to access admin lab endpoints when not local dev
# Set this in localStorage as LAB_ADMIN_KEY or pass as header x-lab-key
LAB_ADMIN_KEY=dev-secret-key
